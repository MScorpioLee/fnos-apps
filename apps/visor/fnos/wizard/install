[
    {
        "stepTitle": "安装说明",
        "items": [
            {
                "type": "tips",
                "helpText": "<b>Orion Visor</b> 是 dromara 开源的现代化堡垒机平台 + 自动化运维工具，支持 SSH/RDP/VNC 在线终端、文件管理、批量操作、计划任务、主机监控等。<br><br><b>⚠️ 资源要求</b>：本应用是 6 服务 docker 栈（ui + service + mysql + redis + influxdb + guacd），首次启动 ~2-3 分钟，<b>飞牛 NAS 4GB 内存以上推荐</b>。低配机型可能跑不起来。<br><br><b>⚠️ 首次登录</b>：默认账号密码均为 <code>admin/admin</code>。<b>登录后请立即修改</b>。数据库等内部服务密码由本 fpk 自动生成随机值，存储在 <code>${TRIM_PKGVAR}/CREDENTIALS.txt</code>。<br><br><b>镜像源</b>：使用 <code>lijiahangmax/orion-visor-*</code> Docker Hub 多架构镜像。如拉取失败，飞牛 NAS 设置里换 docker 加速源后重装。"
            }
        ]
    }
]

#!/bin/sh
APP_DIR="${TRIM_APPDEST}"
APP_DATA_DIR=$1

DATA_DIR="$APP_DATA_DIR/data"
mkdir -p "$DATA_DIR"

cd "$APP_DIR" || exit 1
exec ./metatube-server -port "${TRIM_SERVICE_PORT}" -dsn "file:${DATA_DIR}/metatube.db?cache=shared"
